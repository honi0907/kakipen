# ロゴ画像フォルダ

このフォルダに PNG などの画像を入れると、子機の外部出力起動時に cover（全画面オーバーレイ）として表示されます。

## 対応フォーマット

PNG（推奨）/ JPG / WEBP / GIF

## 複数画像について

複数枚ある場合、ファイル名の昇順で先頭 1 枚が使われます。

## ロゴが無い場合

外部出力は cover なしで書き画面を即表示します（GO 前は暗幕 cover で非表示にできます）。
