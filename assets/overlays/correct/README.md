# 正解オーバーレイ画像フォルダ

親機コンパネで席を「正」に設定して **判定GO** を押すと、子機タブレット・外部出力・コンパネプレビューに表示されます。

## 対応フォーマット

PNG（推奨・透過対応）/ JPG / WEBP / GIF

## ファイル名に `fill` を含む場合

背景全体に合成表示されます（例: `aka_fill.png`）。

## デフォルト

画像未指定時は `/overlays/correct/aka_fill.png` が使われます。
